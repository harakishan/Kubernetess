apiVersion: v1
kind: ReplicationController
metadata:
  name: rc-first-javaapp
  labels: 
    version:  v1
spec:
  replicas: 5
  selector:
    app:  javapplication
  template:
    metadata: 
      labels: 
        app:  javapplication
    spec:
      containers:
        - name: java-container
          image:  maha4iac/mymahalogin:1
          ports:  
            - containerPort: 8080
